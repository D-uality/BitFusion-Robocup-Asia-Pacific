#include "Functions.h"

void test() {
  // analogWrite(grabClawPin, clawUp);
  // delay(1000);
  // analogWrite(liftClawPin, clawGrab);
  // delay(3000);
  // analogWrite(grabClawPin, clawGrab);
  // delay(1000);
  // analogWrite(liftClawPin, clawUp);
  // delay(1000);
  // analogWrite(liftClawPin, clawGrab);
  // delay(1000);
  // analogWrite(grabClawPin, clawUp);
  // delay(1000);
  // mode = 's';
}
